// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "codify-purple",
    },
    secondary: {
      main: "codify-lavender",
    },
  },
});

export default theme;
